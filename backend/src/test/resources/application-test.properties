# Perfil usado pelo GitHub Actions para rodar os testes contra o
# servico Postgres do proprio workflow (ver .github/workflows/backend-ci.yml).
# Ativado via SPRING_PROFILES_ACTIVE=test.
spring.datasource.url=jdbc:postgresql://localhost:5432/gerenciador_musica_test
spring.datasource.username=postgres
spring.datasource.password=postgres
